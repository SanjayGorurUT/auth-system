# 🛡️ Node.js JWT Authentication with MongoDB

A simple authentication system built with **Node.js**, **Express**, **JWT**, and **MongoDB**. Supports user registration, login, and access to protected routes.

---

## 🚀 Features

- User registration with hashed passwords (using bcrypt)
- Login with JWT token generation
- Middleware-protected routes
- MongoDB for persistent storage
- Clean, modular Express structure

---

## 🧰 Tech Stack

- Node.js
- Express
- MongoDB + Mongoose
- JSON Web Tokens (JWT)
- bcrypt for password hashing
- dotenv for environment config

---

## 🏗️ Setup & Installation

### 1. Clone the repo

```bash
git clone https://github.com/your-username/auth-system.git
cd auth-system
